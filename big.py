import turtle
from turtle import *
t = Turtle()
t.shape('turtle')
for i in range(60):
    def square(X):
        length = 5
        def length(length):
         for i in range(4):
                    t.forward(length)
                    t.left(90)
        for i in range(4):
            t.forward(length)
            t.left(90)
    square(100)
    t.right(5)
    length += 5+5
    print(i)
turtle.done()